// backend > src > utilitarios > formatarValor.js

const formatarValor = (valor) => {
  // Lógica para formatar o valor (exemplo)
  return parseFloat(valor).toFixed(2);  // Exemplo de formatação para dois decimais
};

// Exporte a função corretamente
module.exports = formatarValor;
